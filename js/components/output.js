/**
 * output.js — Output Viewport & Copy Button
 * 
 * Use case: Manages the readonly textarea where generated text
 * appears and the "Copy Text" button. On click, copies output to
 * clipboard and signals a toast notification. Reads text from the
 * DOM — no state dependency.
 */

export function mountOutput() {
  // Wire #copy-btn click → clipboard API → toast fade
}
